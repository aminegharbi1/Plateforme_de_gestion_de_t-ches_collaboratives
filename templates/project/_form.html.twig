{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="mt-3">
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        <a href="{{ path('project_index') }}" class="btn btn-outline-secondary ms-2">Cancel</a>
    </div>
{{ form_end(form) }}
