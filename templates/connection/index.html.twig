{% extends 'base.html.twig' %}

{% block title %}Connection - Task Manager{% endblock %}

{% block styles %}
    <link rel="stylesheet" href="{{ asset('css/connection.css') }}">
{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h3 class="mb-3">Connection</h3>
                    <p class="text-muted">Use the links below to sign in or manage your account.</p>

                    <div class="d-grid gap-2">
                        <a href="{{ path('app_login') }}" class="btn btn-primary">Sign in</a>
                        <a href="{{ path('app_home') }}" class="btn btn-outline-secondary">Back to Home</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
